# 第一阶段测试计划

## 后端

- 注册成功、重复用户名、错误密码与登录成功
- 未登录401、学生调用管理接口403，且均符合统一响应
- JWT访问当前用户，不返回密码哈希
- 资源按关键词、分类、城市分页筛选，只返回PUBLISHED
- 草稿详情不可见，公开详情浏览数原子增加
- 管理员可新增资源
- 演示账号SQL哈希与README密码匹配

## 前端

- 登录成功后Pinia与localStorage一致
- 退出后清除登录态
- TypeScript严格类型检查和Vite生产构建

后续工作包将追加收藏幂等、计划归属、AI降级、重复审核、成就幂等、统计数据边界和4条Playwright场景。

