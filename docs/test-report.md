# 第一阶段测试报告

执行日期：2026-08-31

| 范围 | 命令 | 结果 |
|---|---|---|
| 后端单元与集成测试 | `mvn.cmd test` | 25条通过，0失败、0错误、0跳过 |
| 前端单元测试 | `npm.cmd test` | 9条通过 |
| 前端类型/生产构建 | `npm.cmd run build` | 通过 |

已验证：统一响应、401/403、注册登录、JWT、资源筛选、文化地图轻量数据与筛选、收藏幂等、计划归属与节点排序、AI关闭时规则降级、非法AI输出重试、资源白名单与时间重叠校验、打卡图片签名校验与读取、重复打卡限制、管理员不可使用学生打卡接口、仅管理员审核、审核幂等、驳回后重新提交、积分和计划进度事务联动、前端API契约和登录态持久化。

已知非阻塞项：Element Plus当前全量注册使主包约1 MB，后续采用按需导入。MySQL服务需要本机凭据，本轮未执行会重建业务表的真实MySQL导入；SQL初始化边界由H2兼容测试覆盖，联调前仍需按README在MySQL 8执行一次。
